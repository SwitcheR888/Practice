'use strict';
const nameMentor = 'G';
const service = 'teach me';
const conditions = 'agreement';

const message = `${nameMentor} is my teacher. His duties are ${service} HTML, CSS, JavaScript, etc. We have ${conditions} with him.`;
console.log(message);

const studentName = 'Kyryl';
const lastName = 'Fedorovskyi';

const initials = `${studentName[0]}.${lastName[0]}.`;
console.log(initials);

const lengthMessage = `${studentName} has ${studentName.length} letters`;
console.log(lengthMessage);
